import styles from "./index.module.scss";

const Footer = () => {
  return (
    <div className={styles.footer}>
      <img src="/public/git.svg" alt="git-icon" width={25} />
    </div>
  );
};

export default Footer;
